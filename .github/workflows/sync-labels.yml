name: Sync Labels

on:
  push:
    branches:
      - main
      - master
    paths:
      - '.github/labels.yml'
  workflow_dispatch:

jobs:
  sync-labels:
    runs-on: ubuntu-latest
    permissions:
      issues: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync Labels
        uses: srealmoreno/label-sync-action@v1
        with:
          token: ${{ github.token }}
          config-file: .github/labels.yml
          delete-other-labels: true   # ← 开启删除模式